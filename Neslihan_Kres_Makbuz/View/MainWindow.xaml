<Window x:Class="Neslihan_Kres_Makbuz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Neslihan_Kres_Makbuz"
        xmlns:config="clr-namespace:Neslihan_Kres_Makbuz.Config"
        mc:Ignorable="d"
        Title="Harikalar Diyarı Neslihan Kreş" Height="450" Width="1200"               
        DataContext="{Binding Main, Source={StaticResource Locator}}"
        >
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding Version}" />
            <TextBox Text="{Binding Global.KDV, Mode=TwoWay, NotifyOnTargetUpdated=True, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Text="{Binding SelectedStudent.Name}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <DataGrid 
                ItemsSource ="{Binding Students}" 
                SelectedItem="{Binding SelectedStudent}" 
                Height="400"
                AlternatingRowBackground="Bisque"
                SelectionMode="Extended"
                AutoGenerateColumns="False"
                >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="İsim" Binding="{Binding Name}" Width="200" ></DataGridTextColumn>
                    <DataGridTextColumn Header="Ücret" Binding="{Binding Fee}" Width="70"></DataGridTextColumn>
                    <DataGridTextColumn Header="KDV Hariç" IsReadOnly="True" Binding="{Binding Fee_wo_kdv}" Width="70"></DataGridTextColumn>
                    <DataGridTextColumn Header="Kesilen KDV" IsReadOnly="True" Binding="{Binding Cuted_kdv}" Width="40"></DataGridTextColumn>
                </DataGrid.Columns>
                <DataGrid.RowDetailsTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0" Background="BlanchedAlmond" Padding="10">
                            <StackPanel Orientation="Vertical">
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel Orientation="Horizontal" Margin="10">
                                        <TextBlock FontWeight="Bold" Text="TC: " VerticalAlignment="Center" />
                                        <TextBox Foreground="MidnightBlue" Text="{Binding TC, Mode=TwoWay}" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="10">
                                        <TextBlock FontWeight="Bold" Text="Sınıf: " VerticalAlignment="Center" />
                                        <TextBlock Foreground="MidnightBlue" Text="{Binding SClass}" VerticalAlignment="Center" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="10">
                                        <TextBlock FontWeight="Bold" Text="Durum: " VerticalAlignment="Center" />
                                        <TextBlock Foreground="MidnightBlue" Text="{Binding Status}" VerticalAlignment="Center" />
                                    </StackPanel>
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="10">
                                    <TextBlock FontWeight="Bold" Text="Adres: " VerticalAlignment="Center" />
                                    <TextBox Foreground="MidnightBlue" Text="{Binding Address, Mode=TwoWay}" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" />
                                </StackPanel>
                                
                                <StackPanel Orientation="Horizontal" Margin="10">
                                    <TextBlock FontWeight="Bold" Text="Program: " VerticalAlignment="Center" />
                                    <TextBox Foreground="MidnightBlue" Text="{Binding Program_desc, Mode=TwoWay}" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" />
                                </StackPanel>

                                <DataGrid 
                                    ItemsSource ="{Binding Receipts}" 
                                    IsReadOnly="True"
                                    Height="75"                                    
                                    AlternatingRowBackground="Bisque"                                                                        
                                    />

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </DataGrid.RowDetailsTemplate>
            </DataGrid>
            <DataGrid 
                ItemsSource ="{Binding Receipts}" 
                SelectedItem="{Binding SelectedReceipt}" 
                Height="400"
                AlternatingRowBackground="Bisque"
                SelectionMode="Extended"
                />

        </StackPanel>
    </StackPanel>
    <!--<DockPanel LastChildFill="True" >

        <ListBox DockPanel.Dock="Left" ItemsSource="{Binding EmployeeList}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedEmployee}" Width="100" />

        <DockPanel DockPanel.Dock="Bottom">
            <Button DockPanel.Dock="Left" Content="Load Employees" Width="200" Height="25" Command="{Binding LoadEmployeesCommand}" />
            <Button DockPanel.Dock="Right" Content="Save Employees" Width="200" Height="25" Command="{Binding SaveEmployeesCommand}" />
        </DockPanel>

        <Grid>
            <Grid.Resources>
                <Style TargetType="{x:Type TextBox}">
                    <Setter Property="Height" Value="25" />
                    <Setter Property="Margin" Value="10,10,0,0" />
                </Style>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="ID" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedEmployee.ID}" />

            <Label Grid.Row="1" Grid.Column="0" Content="Name" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedEmployee.Name}" />

            <Label Grid.Row="2" Grid.Column="0" Content="Age" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedEmployee.Age}" />

            <Label Grid.Row="3" Grid.Column="0" Content="Salary" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedEmployee.Salary}" />
        </Grid>
    </DockPanel>-->
</Window>
